export const AppConfig = {
  apiBaseUrl: 'http://127.0.0.1:8000/api', // Base URL for API
  chatEndpoint: '/chat-completion',       // Endpoint for chat completion
  defaultModel: 'gpt-3.5-turbo',          // Default model (if applicable)
  requestTimeout: 5000,                   // Timeout for API requests in milliseconds
};
